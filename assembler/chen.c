#include <stdio.h>
int main()
{
    // int (*p) ();
    // p();}p(){
    // printf("%p\n",***********************p);
    // printf("%p %p %p \n", &p, p, *p);
    int arr[5];
    printf("size of arr: %d\n",sizeof(arr));

    return 0;
}